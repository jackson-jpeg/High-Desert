{
  "name": "highdesert-mirror",
  "private": true,
  "type": "module",
  "description": "Same-origin range gateway for High Desert episodes when archive.org is down: a bounded cache, filled by webseed and BitTorrent.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test --test-concurrency=1 test/"
  },
  "dependencies": {
    "webtorrent": "3.0.21"
  }
}
