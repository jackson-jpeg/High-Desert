ANTHROPIC_API_KEY=
# Required for admin API routes (AI categorization). Set in Vercel env vars for production.
ADMIN_API_TOKEN=
# Public token sent by the client — must match ADMIN_API_TOKEN above.
NEXT_PUBLIC_ADMIN_TOKEN=
