"use client";

/**
 * The phone lines. A placeholder until the chat lands from `live/chat`, which
 * replaces this file at this exact path with the real `LiveChat` (no props).
 * The Live screen imports it from here and mounts it beside the player on
 * desktop and in a sheet on mobile.
 */
export function LiveChat() {
  return <div data-testid="live-chat-slot" />;
}
